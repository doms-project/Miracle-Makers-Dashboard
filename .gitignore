# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# 🔴 ROUND 155 — write-only proof artefacts.
# Every one is a `page.screenshot({path})` output from a browser proof: written
# by the proof, read by nothing, regenerated whenever that proof runs. Eleven of
# them were 1.68 MB — 52% — of the round archive, and they grow by one with
# every browser proof added.
scripts/*.png

# tsc incremental cache. Regenerated by the next `tsc --noEmit`; it was only
# ever in the archive because it was not listed here.
*.tsbuildinfo
